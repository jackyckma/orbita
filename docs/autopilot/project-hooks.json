{
  "_doc": "Optional project hooks for Autopilot scripts. Safe to customize; not overwritten blindly on methodology sync if listed in METHODOLOGY.lock customized_files.",
  "prod_smoke_cmd": "curl -fsS https://api.get-orbita.com/v1/health | grep -q '\"status\":\"ok\"'",
  "notes": "Orbita prod smoke: health must return status ok. Zeabur auto-deploys main — allow ~2–5 min after merge before WATCHDOG."
}
