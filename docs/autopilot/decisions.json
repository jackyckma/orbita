{
  "_schema": {
    "decision": {
      "id": "D-001",
      "title": "question",
      "type": "product-direction | contract | compliance",
      "context": "why human input is needed",
      "options": [{ "id": "A", "label": "...", "implication": "..." }],
      "recommendation": "option + why",
      "default_if_silent": "A or null",
      "opened_at": "YYYY-MM-DD",
      "sla_days": 3,
      "unblocks": ["T-0002"],
      "status": "open | decided",
      "decided_option": null,
      "decided_note": null
    },
    "autonomy": "log_veto — reversible calls are decided by Maker and logged in backlog feedback; only product-direction / contract / compliance land here."
  },
  "decisions": [
    {
      "id": "D-001",
      "title": "How should personal-jacky project notes be seeded?",
      "type": "product-direction",
      "context": "Claude Desktop MCP works; portfolio notes (ViOS, melody-thesis, …) are still thin. Autopilot should not invent personal content.",
      "options": [
        {
          "id": "A",
          "label": "Founder + Claude Desktop seed manually (recommended)",
          "implication": "E-05 stays proposed; no Maker tasks until you ask."
        },
        {
          "id": "B",
          "label": "Approve a Maker task that only creates stub notes with titles + links from project-registry.md",
          "implication": "Low-risk stubs; you fill bodies later."
        },
        {
          "id": "C",
          "label": "Defer indefinitely",
          "implication": "E-05 paused."
        }
      ],
      "recommendation": "A — content quality matters more than stubs for a personal knowledge hub.",
      "default_if_silent": "A",
      "opened_at": "2026-08-07",
      "sla_days": 7,
      "unblocks": [],
      "status": "decided",
      "decided_option": "A",
      "decided_note": "Founder 2026-08-07: seed via Claude Desktop / manual; Autopilot must not invent personal note bodies."
    },
    {
      "id": "D-002",
      "title": "Which credentials does the hub get, and scoped how?",
      "type": "compliance",
      "context": "E-09 collectors need two credentials the founder must create by hand. (1) A GitHub token that can READ jackyckma/{orbita,ai-transformation-io,powerhouse,ai-business,ViOS} — three are private, and an unauthenticated read returns 404, which a naive collector would record as 'no activity' rather than 'no access'. (2) A Zeabur API key for https://api.zeabur.com/graphql. NO BUILD WAS REQUIRED: the credential vault already existed and runs in production — packages/lane-credentials, mounted at POST /v1/admin/credentials, write-once (409 on duplicate client_id+name), AES-encrypted with ORBITA_SECRETS_KEY, secret never returned by any read route. Agents resolve secrets server-side via resolveCredentialSecret(clientId, name), already wired into the agent turn runner.",
      "options": [
        {
          "id": "A",
          "label": "Fine-grained GitHub PAT (contents: read-only, those 5 repos) + Zeabur API key (recommended)",
          "implication": "Least privilege available on both platforms. The Zeabur key is still account-wide — a platform limit, not a choice — so treat it as sensitive and rotate if the hub is ever exposed."
        },
        {
          "id": "B",
          "label": "GitHub token only; defer Zeabur",
          "implication": "Line 1 works for all five projects; the merged-but-never-deployed blind spot stays open. T-0052 blocks."
        },
        {
          "id": "C",
          "label": "Use a GitHub App instead of a PAT",
          "implication": "Better rotation and per-repo scoping, but meaningfully more setup for a single-operator portfolio."
        }
      ],
      "recommendation": "A — B leaves open the specific failure that is hardest to notice by eye (build fails, task still says done, prod smoke passes on the previous deployment). C is the right answer at larger scale, not at five repos with one operator.",
      "default_if_silent": null,
      "opened_at": "2026-08-07",
      "sla_days": 7,
      "unblocks": ["T-0051", "T-0052"],
      "status": "decided",
      "decided_option": "A",
      "decided_note": "Founder 2026-08-09: both credentials created and stored under client_id personal-jacky, and verified present via GET /v1/admin/credentials — github_read (scopes: repo:read) and zeabur_api (scopes: deploy:read), both created 2026-08-09T18:45Z. GitHub token is a fine-grained PAT limited to the five registered repos with Contents=Read-only. T-0051 and T-0052 are unblocked and can now be verified against real data rather than fixtures. NOTE FOR THE IMPLEMENTER: resolve secrets by name via resolveCredentialSecret(db, secretsKey, 'personal-jacky', 'github_read' | 'zeabur_api'). Never read them into a log, a report body, an error message, or a committed file. A 404 from a registered private repo means the token lost access — surface it as a collector FAILURE, never as an empty report.",
      "_procedure": {
        "_doc": "CANONICAL credential names live here, not duplicated into task descriptions — T-0051/T-0052 unblock from this decision, so they inherit these. If a name changes, change it here only.",
        "endpoint": "POST https://api.get-orbita.com/v1/admin/credentials",
        "auth_header": "x-orbita-admin-token: $ORBITA_ADMIN_TOKEN",
        "client_id": "personal-jacky",
        "write_once": "Duplicate client_id+name returns 409. To replace a credential you must delete the row first — there is no update route. NOTE: a fine-grained PAT's permissions can be edited on GitHub without regenerating the token, so widening or narrowing access does NOT require touching the vault row.",
        "credentials": [
          {
            "name": "github_read",
            "scope": ["repo:read"],
            "used_by": "T-0051 git collector",
            "status": "STORED 2026-08-09",
            "source": "GitHub > Settings > Developer settings > Personal access tokens > Fine-grained. Repository access: only jackyckma/{orbita,ai-transformation-io,powerhouse,ai-business,ViOS}. Permission: Contents = Read-only. Nothing else."
          },
          {
            "name": "zeabur_api",
            "scope": ["deploy:read"],
            "used_by": "T-0052 zeabur collector",
            "status": "STORED 2026-08-09",
            "source": "https://zeabur.com/account/api-keys > Generate new API key."
          }
        ],
        "verify": "GET https://api.get-orbita.com/v1/admin/credentials with the same admin header — returns metadata only. Secrets are never returned; seeing the two names listed is the confirmation."
      },
      "_why_read_only": {
        "_doc": "Founder asked 2026-08-09 whether to grant Contents:write now, since instruction dispatch into each repo is coming (E-07 H3). Decision: NO. Recorded so a future session does not widen this token by reflex.",
        "reasons": [
          "DIFFERENT TRIGGER, DIFFERENT RISK. github_read is used by an UNATTENDED job on a schedule. Every repo it touches auto-deploys to production on merge to main. An unattended scheduled process holding write to five production repos is the single worst place to put that permission. Dispatch, by contrast, only ever runs after a founder gate.",
          "NOTHING WRITES YET. Every task in E-09 (T-0050..T-0053) is read-only by design. E-07 H3 (dispatch) is not even an approved epic — it has not been decomposed.",
          "THE GATE IS AT THE CONVERSATION LAYER BY DESIGN (portfolio-hub.md). Writes are supposed to happen with a human present. That path already works today: Claude writes each repo's backlog during a session with the founder. Automating it is an optimization, not a missing capability — and a scheduled unattended writer would move the gate to the wrong place.",
          "BLAST RADIUS. Read-only means a buggy or looping collector produces a wrong report. Write means wrong commits on main of five auto-deploying repos. The first is a bad afternoon; the second is a bad week.",
          "UPGRADING IS CHEAP. A fine-grained PAT's permissions can be edited without regenerating the token, so the stored secret stays valid and the write-once vault row does NOT need replacing. There is no lock-in penalty for starting narrow."
        ],
        "when_dispatch_ships": "Create a SEPARATE credential named github_dispatch with contents:write (and pull_requests:write if dispatch goes via PR), used only by the gated dispatch path — never by the collectors. Note that fine-grained PATs cannot scope to paths: any write token can modify source code, not just docs/autopilot/*.json. That is a further argument for PR-based dispatch, so the target repo's own Checker reviews what the hub proposes rather than the hub pushing straight to main."
      }
    }
  ]
}
