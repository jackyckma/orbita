{
  "_schema": {
    "purpose": "Founder-approved product direction. Maker REPLAN only decomposes status:approved epics.",
    "epic": {
      "id": "E-01",
      "title": "one line",
      "goal": "outcome",
      "status": "approved | proposed | done | paused",
      "phase": "optional",
      "decomposes_into": ["T-0001"],
      "decisions": [],
      "notes": "constraints / flags"
    }
  },
  "epics": [
    {
      "id": "E-01",
      "title": "Bootstrap Autopilot",
      "goal": "Prove Maker/Checker can run one docs-only task end-to-end.",
      "status": "done",
      "phase": "bootstrap",
      "decomposes_into": ["T-0001"],
      "decisions": [],
      "notes": "Completed by founder session 2026-08-07 (scaffolds verified; stale audit PRs closed)."
    },
    {
      "id": "E-02",
      "title": "W35 notes export API",
      "goal": "Callers can export a client’s notes as Obsidian-friendly Markdown with wikilinks from note_links.",
      "status": "done",
      "phase": "w35",
      "decomposes_into": ["T-0010", "T-0011", "T-0012"],
      "decisions": [],
      "notes": "Additive GET only. No AT graph dogfood in this epic (needs L2 rejection data). Bump API to 0.0.1-w35 already used for PA1.5 OAuth — use 0.0.1-w36 for this ship."
    },
    {
      "id": "E-03",
      "title": "Ops: AT1b cron visibility",
      "goal": "Operator can confirm supply+poll harnesses are still ticking without Dashboard archaeology.",
      "status": "done",
      "phase": "l2-ops",
      "decomposes_into": ["T-0020"],
      "decisions": [],
      "notes": "Read-only report + optional script. Do not change harness cron without founder. L2 dogfood resume deferred 2026-08-07 (founder mid-term portfolio hub takes priority for attention)."
    },
    {
      "id": "E-04",
      "title": "note_search empty-results investigation",
      "goal": "Document why prod note_search can return empty while GET-by-id works; fix if a clear env/code bug.",
      "status": "approved",
      "phase": "l1-hardening",
      "decomposes_into": ["T-0030", "T-0060"],
      "decisions": [],
      "notes": "REOPENED 2026-08-16 after the PR #8 fix failed to resolve the symptom. DIAGNOSIS UPDATED 2026-08-25 (claude-hub-session) — READ THIS BEFORE INVESTIGATING, the previous next-step in this field was wrong. (1) RULED OUT: 'check whether MINIMAX_API_KEY is set in prod' — it IS set. Verified directly against Zeabur orbita-api production env. Also present: MINIMAX_BASE_URL=https://api.minimax.io/v1, MINIMAX_MODEL=MiniMax-M3. Do not spend another cycle on this hypothesis. (2) NOT SET in prod but harmless: EMBEDDING_MODEL and EMBEDDING_DIMENSIONS both have defaults in packages/lane-memory/src/config.ts ('embo-01' and 1024). MINIMAX_GROUP_ID is absent and has NO default. (3) THE REAL BLOCKER: packages/lane-memory/src/embed.ts has SIX separate `return null` paths — missing key/empty text, !response.ok, base_resp.status_code !== 0, no vector, vector.length !== EMBEDDING_DIMENSIONS, and a bare catch — with ZERO logging. All six produce an identical silent null, the note is then stored with embedding=NULL, and the write reports success. So one symptom maps to five distinct root causes and the code emits no evidence of which. This is the same failure CLASS the PR #8 fix was meant to remove: that fix corrected the request protocol but kept the silence, which is why re-PUTting all 14 notes and confirming deploy freshness narrowed nothing. (4) DO THIS FIRST — instrument before fixing: make embedText emit a discriminated reason per path (HTTP status, base_resp.status_code + status_msg, actual vs expected vector length). Never log the API key or the text body. One live call then answers it outright. Fixing before instrumenting risks a third round of 'merged, still broken'. Leading candidates once instrumented: MINIMAX_GROUP_ID required and absent (the code's own comment says some MiniMax regions require it), 'embo-01' retired or wrong, or a dimension mismatch against the hardcoded 1024 default. Consider a startup assertion that an embedding round-trip works, so this fails loudly at deploy rather than silently at write time. Full write-up: personal-jacky note a21b8bf5-c253-4bb6-bee3-7f4c33c50238. BLOCKS THE HUB: until search is trustworthy, sessions resume via flat memory keys, not semantic search."
    },
    {
      "id": "E-05",
      "title": "Personal steward project notes seed",
      "goal": "Seed high-value personal-jacky notes for ViOS / melody-thesis / etc.",
      "status": "proposed",
      "phase": "pa0-content",
      "decomposes_into": [],
      "decisions": ["D-001"],
      "notes": "D-001 → A (2026-08-07): founder + Claude Desktop seed manually. Keep proposed; no Maker tasks."
    },
    {
      "id": "E-06",
      "title": "W35b AT graph dogfood (rubric ↔ draft edges)",
      "goal": "Link editorial rubric notes to rejected draft notes in AT client graph.",
      "status": "paused",
      "phase": "w35",
      "decomposes_into": [],
      "decisions": [],
      "notes": "Blocked on L2 green + real rejection corpus. Re-approve when dogfood resumes."
    },
    {
      "id": "E-07",
      "title": "Portfolio hub — multi-project reports + instruction dispatch",
      "goal": "Orbita as Jacky’s central hub: ingest project reports, brief via chatbot, dispatch agent instructions into each repo for Cursor Autopilot.",
      "status": "proposed",
      "phase": "midline",
      "decomposes_into": [],
      "decisions": [],
      "notes": "Umbrella vision — stays proposed so Maker cannot invent hub direction. Sketch: docs/personal-steward/portfolio-hub.md. Decompose by approving concrete slices: E-08 (H2 retrieval), E-09 (collection), E-11 (on-demand trigger), E-12 (on-demand GitHub read) are approved slices; E-13 is activation. Remaining: instruction dispatch (H3, still proposed, needs github_dispatch credential — see D-002 _why_read_only)."
    },
    {
      "id": "E-08",
      "title": "Notes list/filter for hub briefs",
      "goal": "Agents can list notes by project, type, and date range without burning a reasoning loop or losing state across chat sessions.",
      "status": "done",
      "phase": "hub-h2",
      "decomposes_into": ["T-0040", "T-0041"],
      "decisions": [],
      "notes": "First approved slice of E-07 (portfolio-hub.md H2). Additive read-only API; do NOT remove or change semantic /notes/search. Founder rationale 2026-08-07: chat sessions get rotated to avoid context overflow, so durable cross-project state must be queryable out of Orbita by a fresh session."
    },
    {
      "id": "E-09",
      "title": "Portfolio collectors — pull the three report lines into Orbita",
      "goal": "Orbita collects autopilot, deployment and (later) runtime state for every registered project on a schedule, so one agent session can brief across the portfolio without crawling repos and dashboards by hand.",
      "status": "done",
      "phase": "hub-h1",
      "decomposes_into": ["T-0050", "T-0051", "T-0052", "T-0053"],
      "decisions": ["D-002"],
      "notes": "ARCHITECTURE (decided, do not relitigate): collection lives in ORBITA, not in each product. Rationale — (1) the autopilot line is already committed to git by Maker/Checker every run; (2) the deploy line lives in Zeabur, one shared platform, one credential for all projects; (3) only the runtime line genuinely requires the app. Consequence: lines 1+2 cover every project with ZERO work in any product repo, and onboarding a new project is a registry row. CORRELATION KEY: git commit sha ties the lines together. Runtime line stays E-10 (proposed) until a product has real signals. STATUS 2026-08-29: collector code merged (T-0050..T-0053 done). Runtime activation (harness rows, verify scripts) tracked in E-13."
    },
    {
      "id": "E-10",
      "title": "Runtime report endpoints (per project)",
      "goal": "Live products expose their own operational signals (signups, logins, queue depth) so the hub can see whether shipped work is actually being used.",
      "status": "proposed",
      "phase": "hub-h1",
      "decomposes_into": [],
      "decisions": [],
      "notes": "The only line that genuinely needs per-project work — an app's own database is the only source. Deliberately NOT approved: no product has meaningful runtime signals yet. Building endpoints now buys a public surface and a credential per project in exchange for reporting zeroes. Approve per project when that project has users. Contract: ai-dev-methodologies portfolio-hub-reporting.md, edge=runtime."
    },
    {
      "id": "E-11",
      "title": "On-demand automation trigger — hub session can nudge a Cursor Automation early",
      "goal": "From a hub chat session, Claude can trigger this repo's Maker or Checker to run now instead of waiting for its next scheduled tick.",
      "status": "done",
      "phase": "hub-h3-pilot",
      "decomposes_into": ["T-0054", "T-0055"],
      "decisions": [],
      "notes": "Founder approved 2026-08-16, orbita ONLY as a pilot — do not generalise to other projects in this epic. MECHANISM (mirrors D-002, reuse packages/lane-credentials as-is): founder creates the Cursor Automation webhook URL + key himself in the Cursor UI and stores them via POST /v1/admin/credentials under client_id personal-jacky — Claude/Maker must NEVER see, enter, or transmit the raw webhook key. Credential names: cursor_webhook_orbita_maker, cursor_webhook_orbita_checker. HARD CONSTRAINTS: orbita only; read-only toward repo content; no polling/retry/queueing; MCP-tool surface only; an agent must NEVER call trigger_automation on its own initiative — founder-requested every time."
    },
    {
      "id": "E-12",
      "title": "On-demand GitHub read tools over MCP — hub session can query repo state from any device",
      "goal": "From ANY hub session — including mobile, which has no first-party GitHub connector — Claude can read a file, list recent commits, or list pull requests for any registered project.",
      "status": "approved",
      "phase": "hub-h3-pilot",
      "decomposes_into": ["T-0056", "T-0057"],
      "decisions": [],
      "notes": "Founder approved 2026-08-17, motivated by a mobile session where GitHub's own connector was unavailable. LOWER RISK THAN E-11: read-only toward GitHub with no path to mutation, so no single-project pilot needed. REUSES the existing github_read credential from D-002 as-is — no new credential, no founder setup step. SCOPE: github_get_file, github_list_commits, github_list_pull_requests. HARD CONSTRAINTS: strictly read-only — never call a mutating GitHub endpoint; MCP-tool surface only; never let the token reach a log, error message, tool response, or committed file. NOT IN SCOPE: any write/mutating GitHub action (stays chat-session-only via the desktop connector — this is intentionally NOT a path around that boundary); code search; scheduling. VERIFIED WORKING END-TO-END 2026-08-25 from a hub session: all three tools used to read orbita source and history without the desktop GitHub connector. T-0057 (tests) still open — see E-13."
    },
    {
      "id": "E-13",
      "title": "Hub activation — make the collectors actually run, and unstick the review lane",
      "goal": "portfolio_brief returns real collected reports instead of 'no report ever collected', and PRs stop dying of stale locks.",
      "status": "approved",
      "phase": "hub-h1",
      "decomposes_into": ["T-0058", "T-0059", "T-0052", "T-0057", "T-0061"],
      "decisions": [],
      "notes": "OPENED 2026-08-25 (claude-hub-session) after a hub session found the hub reads correctly but has never been fed. THREE STRANDS, roughly in priority order.\n\n(1) THE GIT COLLECTOR HAS NEVER RUN. T-0051 merged 2026-08-16; nine days later portfolio_brief reports 'no report ever collected' for all seven registered projects, and note_list shows no report-type notes from any collector. The code IS wired — apps/orbita-api/src/index.ts builds portfolioSystemCollectors with portfolio_git and passes it to BOTH createHarnessRoutes and startHarnessTick. What is missing is the harness ROW: docs/personal-steward/portfolio-collectors.md names a one-time setup step, ./scripts/portfolio-git-collect-setup-harness.sh (client personal-jacky, credential github_read, template portfolio-git-collect@v1), which appears never to have been executed. Determine whether the script can be run by an agent or needs founder credentials from ~/.orbita-personal.env; if it needs the founder, say so plainly and mark needs_human rather than half-running it. THEN VERIFY BY OBSERVATION, not by exit code: call portfolio_brief and confirm at least one project's repo line is no longer stale. Also confirm private-repo reads (powerhouse, ai-business, ViOS) return real data and not a 404 recorded as an empty success.\n\n(2) TWO PRs ARE STUCK AND THE REVIEW LANE IS FAILING A NEW WAY. T-0052 (PR #15, Zeabur collector — rebased, conflicts resolved, acceptance green) and T-0057 (PR #23, github-read tests — TS2322 fixed and pushed) were both auto-escalated to needs_human by FORCE_NEEDS_HUMAN with the SAME reason: 'lock held ~7h with task still in_review — REVIEW kept failing to complete before it could bounce the task itself'. That watchdog is a good addition, but it treats the symptom. The Checker is now STARTING review and not FINISHING it — different from the 2026-08-09 deadlock, which never started. Founder screenshots around 2026-08-16 showed the Automation intermittently Failing rather than idle. Root-cause why REVIEW does not complete (candidate: waiting on CI — PR #15 previously showed an e2e-tier-a CI FAILURE while local verify-all passed, so the review may be blocking on a check that never goes green). Merging the two PRs is worth doing on its own merits once each is re-verified, but do NOT treat merging them as closing this strand — the lane will just stall on the next PR.\n\n(3) ACCEPTANCE CRITERIA COULD NOT HAVE CAUGHT THIS. T-0051's acceptance was typecheck, tests, and two ripgreps — all of which passed while the feature produced nothing observable, because none of them asked whether a report was ever written. For any task whose value depends on a runtime activation step (a harness row, a feature flag, a credential, a cron), acceptance must include evidence of OUTPUT, not just of code existing. Add that as a rule wherever task-writing guidance lives in this repo, and prefer it in future tasks here.\n\nALSO CHECK, low priority: portfolio_brief reports kongstory and tango-atelier as enabled:true, but T-0050's own feedback says they were added as enabled:false for visibility only with no Autopilot loop. Either the registry drifted or the brief is misreporting; whichever it is, the staleness detector is currently raising findings for two projects that were never meant to report."
    }
  ]
}
