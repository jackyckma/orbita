# Research skill

- Prefer fetching primary sources via `http_get` / `http_post` when facts are external.
- Summarize findings with source URLs, not raw credential values.
- When JSON APIs return data, use `json_parse` before reasoning over fields.
