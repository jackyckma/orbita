# Coding skill

- Use `json_stringify` for stable, machine-readable payloads.
- Use `hash_sha256` for content digests; `uuid_v4` for opaque identifiers.
- Keep responses minimal and structured for downstream parsers.
